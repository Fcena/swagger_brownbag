{
  "name": "shopping-list",
  "version": "0.0.1",
  "private": true,
  "description": "Swagger Brownbag - Shopping List",
  "keywords": [],
  "author": "",
  "license": "",
  "main": "dist/app.js",
  "dependencies": {
    "express": "^4.12.3",
    "swagger-express-mw": "^0.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "nodemon": "^1.15.1",
    "should": "^7.1.0",
    "supertest": "^1.0.0"
  },
  "scripts": {
    "swagger-verify": "swagger project verify",
    "swagger-edit": "swagger project edit",
    "swagger-start": "swagger project start",
    "swagger-start-mock": "swagger project start -m",
    "dev": "nodemon --exec babel-node app.js",
    "prestart":
      "babel --copy-files app.js -d dist && babel --copy-files api -d dist/api && babel --copy-files config -d dist/config",
    "start": "node dist/app.js"
  }
}
